<div class="page news-page">
	<header class="sport-header" >The Sports Page</header>
	<section id="news-display">
		<% @result.each do |res|%>
			<div class="news-return">
				<div class="headline">
					<a href="<%= res[:url] %>" class="news-link"><%= res[:headline] %></a>
				</div>
				<div class="news-image">
					<img src='<%= res[:image] %>' >
				</div>
				<hr />
				<div class="news-body">
					<%= res[:body] %> 
				</div>
			</div>
		<% end %>
	</section>
</div>