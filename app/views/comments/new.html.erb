<%= @query.title %>
<%= @query.body %>

<%= semantic_form_for @comment %>
  <%= f.inputs do %>
    <%= f.input :sport_id, as: :select, :include_blank => false, collection: Sport.pluck(:name, :id), label: 'Pick a sport' %>
    <%= f.input :title %>
    <%= f.input :body, input_html: { rows: 10 } %>
    <%= f.input :link %>
  <% end %>
  
  <%= f.actions do %>
    <%= f.action :submit, :as => :input %>
  <% end %>

<% end %>